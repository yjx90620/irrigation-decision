# Irrigation Decision

灌溉决策研究项目仓库。总体方案见 [docs/研究方案.md](docs/研究方案.md)：数字农田与多目标灌溉基准构建 → 多目标协同灌溉决策模型 → 跨区域动态迁移与适应性验证，三个研究内容依次衔接。

## ⚠️ 当前状态：正在修复外部交叉验证发现的一批方法学问题

系统升级阶段（轮作模型）的结果——论文一对比1a/1b/3/4、论文二核心对比——**已确认存在多个真实
bug**（轮作季节交接日期倒流、年度硬配额被绕过、RL域随机化采样偏差、超体积参考点计算错误等），
正在按 [docs/审计修复计划.md](docs/审计修复计划.md) 逐条修复，受影响的数据/模型/图表已移到
`data/processed/invalidated/`、`figures/invalidated/`，**在修复验收通过、结果重新生成之前，
不要引用这些数值作为结论**。下面的"当前进度"表和各论文草稿里对应章节保留了失效前的记录作为
开发历史，正文里都加了醒目标注。

## 当前进度

**权威结果以 [docs/papers/](docs/papers/) 下的三篇小论文草稿为准**（数据/方法/结果/讨论结构，结果部分是多组具名对比实验），下表只是概览；`src/*/README.md` 记录的是各目录脚本本身的用法和踩过的坑，不是最新结果的汇总。

| 研究内容 | 状态 | 详见 |
| --- | --- | --- |
| 一、数字农田与多目标灌溉基准 | 小麦→玉米轮作模型+真实初始墒情（ERA5-Land）已替换早期单季原型；对比1a（α配水扫描，4/4双季站点）、对比2（初始墒情假设偏差）、对比3（NSGA-II/III/MOEA-D）、对比4（阶段边际水价值，5站点）已完成；对比1b（联合优化vs最优固定α，更大预算）跑批中；对比5（CMIP6未来气候）数据已齐、对比实验未跑 | [docs/papers/论文一_数字农田与帕累托优化.md](docs/papers/论文一_数字农田与帕累托优化.md) |
| 二、多目标协同灌溉决策模型 | 环境已升级为轮作+安全层；核心对比（残差RL vs 直接RL vs 阈值规则）训练中——此前反复卡死，根因是 aquacrop-ospy 自身一个无限循环 bug，已定位并修复（见下）,当前用全部5站点重新训练 | [docs/papers/论文二_偏好条件化强化学习决策.md](docs/papers/论文二_偏好条件化强化学习决策.md)、[src/rl/README.md](src/rl/README.md) |
| 三、跨区域动态迁移与适应性验证 | 环境指纹+留一验证+实例加权（单季原型阶段）证实宁夏最难迁移但环境距离不是唯一因素；任务敏感度指标（5/5站点）和"距离×敏感度"两因子风险预测已算出，尚未对照真实迁移差距验证；轮作版留一迁移已写好脚本未跑 | [docs/papers/论文三_跨区域动态迁移.md](docs/papers/论文三_跨区域动态迁移.md) |

### 给外部审阅者（含 AI 交叉验证）的说明

- 这个仓库经历过一次系统性升级（单季模型 → 轮作模型+真实初始墒情+CMIP6+每篇论文的核心创新设计），升级动机和设计见 [docs/系统升级方案.md](docs/系统升级方案.md)。**升级前的原型结果仍保留在仓库和小论文草稿里，但明确标注为"已作废/被替代"**，不要把它们当作当前结论引用。
- 三篇小论文草稿里"仍待决定""已知局限"这类小节是真实的、未完成的开放问题，不是自谦客套——审阅时可以重点看这些地方是否遗漏了什么。
- [docs/aquacrop_patches.md](docs/aquacrop_patches.md) 记录了一个本项目依赖的第三方库（aquacrop-ospy）本身的无限循环 bug 及修复：这是本项目开发过程中反复出现"计算莫名卡死"问题的根因，之前几轮排查中做过的规避（跳过某年份、排除某站点等）大多已经过时并在小论文里更新，但如果审阅时发现某处结果的站点数/年份数少于预期，先检查是不是这个历史遗留问题还没补跑，而不是默认数据本身有缺陷。
- 各研究方向的原始数据和实验结果 csv/训练产物都在 `data/` 下随仓库一起版本控制（不是只能靠脚本重新生成），复现脚本在各 `src/*/` 子目录，运行顺序见对应 README。

## 仓库结构

```
docs/            研究方案与文档
figures/         论文用图（复合图，PNG）
data/
  raw/           下载的原始数据（气象/土壤）
  processed/     实验结果、帕累托前沿、训练产物
src/
  data/          数据获取与站点/土壤配置
  sim/           AquaCrop 基线仿真、灌溉策略、NSGA-II 优化
  rl/            强化学习环境、Gym 封装、PPO 训练与评估
  transfer/      环境指纹、区域相似性分析（跨区域迁移）
  viz/           论文复合图的生成脚本
```

每个子目录都有自己的 README，说明该目录下脚本的用途和运行顺序。**`data/` 下所有原始数据和实验结果都纳入 git 版本控制**（不是只能靠脚本重新生成）——训练结果来之不易（比如 PPO 训练踩过的坑，见 `src/rl/README.md`），保留原始数据是为了论文制图时能随时回来重新组合、深挖，不用重新跑一遍。

## 环境搭建

```bash
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
.venv/Scripts/python patches/patch_aquacrop_higc.py
```

CDS（AgERA5）需要在 `~/.cdsapirc` 配置个人 API key（去 https://cds.climate.copernicus.eu 免费注册获取），非必需——默认数据源是免注册的 Open-Meteo。

`patches/patch_aquacrop_higc.py` 修复了 aquacrop-ospy 自身一个真正的无限循环 bug（严重水分胁迫下作物日历塌缩会让收获指数反推陷入死循环），是本项目多次"卡死"问题的根因，不是可选步骤——`.venv/` 不入库，每次重装环境都要重新跑一次；脚本本身幂等。详见 [docs/aquacrop_patches.md](docs/aquacrop_patches.md)。

## 数据与代码约定

- 原始数据、实验结果、训练产物（csv/zip 等）都纳入 git 版本控制，同时也都能靠 `src/` 下的脚本复现（见各目录 README 里的运行顺序）——两者不冲突，复现脚本是为了能重新生成/扩展，入库是为了不必每次都重新生成。只有真正的临时中间文件（比如 AgERA5 下载过程中的原始 NetCDF 格点文件）才会被 `.gitignore`。
- 长耗时任务（AgERA5 批量下载、基线实验网格、NSGA-II、PPO 训练）都设计成可后台运行、可断点续跑（已完成的会自动跳过）。
- 论文制图脚本在 `src/viz/`，产出图在 `figures/`，见 [src/viz/README.md](src/viz/README.md)。
