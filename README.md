# Irrigation Decision

灌溉决策研究项目仓库。总体方案见 [docs/研究方案.md](docs/研究方案.md)：数字农田与多目标灌溉基准构建 → 多目标协同灌溉决策模型 → 跨区域动态迁移与适应性验证，三个研究内容依次衔接。

## 外部交叉验证与审计修复（2026-08，已完成）

仓库公开后经 AI 交叉验证，发现系统升级阶段（轮作模型）存在多个真实 bug（轮作季节交接日期倒流、
年度硬配额被绕过、RL域随机化采样偏差、超体积参考点计算错误等），**已按
[docs/审计修复计划.md](docs/审计修复计划.md) 逐条核实并修复（25项自动化测试全部通过），受影响
的全部结果已用修复后的代码重新生成并提交**。修复前的旧数据/模型/图表保留在
`data/processed/invalidated/`、`figures/invalidated/` 作开发记录，不作为结论引用；下面的
"当前进度"和各论文草稿都已更新为重新生成后的结果。

## 当前进度

**权威结果以 [docs/papers/](docs/papers/) 下的三篇小论文草稿为准**（数据/方法/结果/讨论结构，结果部分是多组具名对比实验），下表只是概览；`src/*/README.md` 记录的是各目录脚本本身的用法和踩过的坑，不是最新结果的汇总。

| 研究内容 | 状态 | 详见 |
| --- | --- | --- |
| 一、数字农田与多目标灌溉基准 | 对比1a（α配水扫描，4/4双季站点）、对比1b（联合优化vs最优固定α，更大预算）、对比2（初始墒情假设偏差）、对比3（NSGA-II/III/MOEA-D，3个独立seed）、对比4（阶段边际水价值，5站点）全部完成并通过审计修复；对比5（CMIP6未来气候）数据已齐、对比实验尚未开始 | [docs/papers/论文一_数字农田与帕累托优化.md](docs/papers/论文一_数字农田与帕累托优化.md) |
| 二、多目标协同灌溉决策模型 | 核心对比（残差RL vs 直接RL vs 两种规则基线，3个独立seed）审计修复后重新训练完成——RL在所有站点大幅省水且加权回报占优，但对更强规则基线的原始产量不再全面领先，是更保守也更可信的结论 | [docs/papers/论文二_偏好条件化强化学习决策.md](docs/papers/论文二_偏好条件化强化学习决策.md)、[src/rl/README.md](src/rl/README.md) |
| 三、跨区域动态迁移与适应性验证 | 环境指纹+留一验证+实例加权（单季原型阶段）证实宁夏最难迁移但环境距离不是唯一因素；轮作版留一迁移+两因子迁移风险预测首次真正跑通并对照真实迁移差距验证（相关系数0.95 vs 距离单因子0.65，n=5，方向性证据） | [docs/papers/论文三_跨区域动态迁移.md](docs/papers/论文三_跨区域动态迁移.md) |

### 给外部审阅者（含 AI 交叉验证）的说明

- 这个仓库经历过两轮系统性修复：先是单季模型 → 轮作模型+真实初始墒情+CMIP6+每篇论文的核心
  创新设计的升级（[docs/系统升级方案.md](docs/系统升级方案.md)），后是一次外部 AI 交叉验证
  发现的一批真实方法学 bug 的修复（[docs/审计修复计划.md](docs/审计修复计划.md)）。**两轮
  升级/修复之前的结果都仍保留在仓库和小论文草稿里，但明确标注为"已作废/被替代"**，不要把它们
  当作当前结论引用——如果你（包括 AI 审阅者）在复核这个仓库，`docs/审计修复计划.md` 本身就是
  一份可以复用的核实清单模板：里面记录了每条声称问题的具体验证方法（代码位置+实测数据），可以
  按同样的方法论继续深挖有没有遗漏的问题，而不必从零开始。
- 三篇小论文草稿里"仍待决定""已知局限"这类小节是真实的、未完成的开放问题，不是自谦客套——审阅时可以重点看这些地方是否遗漏了什么。
- [docs/aquacrop_patches.md](docs/aquacrop_patches.md) 记录了一个本项目依赖的第三方库（aquacrop-ospy）本身的无限循环 bug 及修复：这是本项目开发过程中反复出现"计算莫名卡死"问题的根因之一，另一个更隐蔽的根因（轮作季节交接的日期倒流）记录在审计修复计划的 P0-1。
- 各研究方向的原始数据和实验结果 csv/训练产物都在 `data/` 下随仓库一起版本控制（不是只能靠脚本重新生成），复现脚本在各 `src/*/` 子目录，运行顺序见对应 README。

## 仓库结构

```
docs/            研究方案与文档
figures/         论文用图（复合图，PNG）
data/
  raw/           下载的原始数据（气象/土壤）
  processed/     实验结果、帕累托前沿、训练产物
src/
  data/          数据获取与站点/土壤配置
  sim/           AquaCrop 基线仿真、灌溉策略、NSGA-II 优化
  rl/            强化学习环境、Gym 封装、PPO 训练与评估
  transfer/      环境指纹、区域相似性分析（跨区域迁移）
  viz/           论文复合图的生成脚本
```

每个子目录都有自己的 README，说明该目录下脚本的用途和运行顺序。**`data/` 下所有原始数据和实验结果都纳入 git 版本控制**（不是只能靠脚本重新生成）——训练结果来之不易（比如 PPO 训练踩过的坑，见 `src/rl/README.md`），保留原始数据是为了论文制图时能随时回来重新组合、深挖，不用重新跑一遍。

## 环境搭建

```bash
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
.venv/Scripts/python patches/patch_aquacrop_higc.py
```

CDS（AgERA5）需要在 `~/.cdsapirc` 配置个人 API key（去 https://cds.climate.copernicus.eu 免费注册获取），非必需——默认数据源是免注册的 Open-Meteo。

`patches/patch_aquacrop_higc.py` 修复了 aquacrop-ospy 自身一个真正的无限循环 bug（严重水分胁迫下作物日历塌缩会让收获指数反推陷入死循环），是本项目多次"卡死"问题的根因，不是可选步骤——`.venv/` 不入库，每次重装环境都要重新跑一次；脚本本身幂等。详见 [docs/aquacrop_patches.md](docs/aquacrop_patches.md)。

## 数据与代码约定

- 原始数据、实验结果、训练产物（csv/zip 等）都纳入 git 版本控制，同时也都能靠 `src/` 下的脚本复现（见各目录 README 里的运行顺序）——两者不冲突，复现脚本是为了能重新生成/扩展，入库是为了不必每次都重新生成。只有真正的临时中间文件（比如 AgERA5 下载过程中的原始 NetCDF 格点文件）才会被 `.gitignore`。
- 长耗时任务（AgERA5 批量下载、基线实验网格、NSGA-II、PPO 训练）都设计成可后台运行、可断点续跑（已完成的会自动跳过）。
- 论文制图脚本在 `src/viz/`，产出图在 `figures/`，见 [src/viz/README.md](src/viz/README.md)。
